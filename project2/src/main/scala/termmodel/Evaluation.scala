package main.scala.termmodel
import scala.collection.mutable.ListBuffer

class Evaluation {
  def precision(query: ListBuffer[String]) = {
    
  }
  
  def recall(query: ListBuffer[String]) = {
    
  }
  
  def f1score(query: ListBuffer[String]) = {
    
  }
}